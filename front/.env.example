# URL base del backend (Django/DRF). La usa el http-client que llega en la Fase 3.
VITE_BACKEND_BASE_URL=http://localhost:8000

# Target del proxy de desarrollo (opcional; se activará cuando el playground haga llamadas reales).
VITE_PROXY_TARGET=http://localhost:8000
