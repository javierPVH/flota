// DX3: la app de campo usa ahora la MISMA gráfica rica que gestión (overlay,
// tooltip, línea de hoy) en vez de la versión mínima que divergía del dato.
export { KmChart } from '@flota/ui/ui'
export type { ChartReading, KmChartOverlay } from '@flota/ui/ui'
